# Core dependencies for rag_library

# Vector store (CPU build is fine for our research scale)
faiss-cpu>=1.7.0

# Gemini client — free-tier-friendly, recommended for prototyping
google-genai>=0.3

# OpenAI client — optional, only needed if using OpenAIGenerator/OpenAIEmbedder
openai>=1.0

# PDF extraction (used by extract.py)
pymupdf>=1.23

# Loading .env files (used by example.py)
python-dotenv>=1.0

# When the Llama generators/embedders are implemented, add their deps here.
# Likely candidates (uncomment when needed):
# torch>=2.0
# transformers>=4.40
# accelerate>=0.30
fairscale>=0.4.13
tiktoken>=0.12.0

# Core ML dependencies
FlagEmbedding>=1.3.2,<1.4

# Explicitly pinned to avoid CUDA mismatches
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0

# Keep Hugging Face stack compatible with torch==2.3.0
transformers==4.44.2
sentence-transformers>=3.0,<4.0
accelerate>=0.30,<1.0

# Pinning NumPy to avoid the 2.0.0 incompatibility we encountered
numpy==1.26.4

# Utility/Math libraries
scipy
scikit-learn
